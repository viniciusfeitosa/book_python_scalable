version: '3'

services:

  application:
    image: application
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1024M
        reservations:
          cpus: '1'
          memory: 512M
    build: ./sanic_app
    volumes:
      - './sanic_app:/app'
    ports:
      - 8080:8080
    restart: always

  web_server:
    build: ./nginx
    ports:
      - "80:80"
    links:
      - application
    depends_on:
      - application
    restart: always
